# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Workflow Rules

- **After completing any planned task:** update any relevant `docs/` files to reflect new files, components, or behavioral changes introduced by the task. **Per-file rationale goes in [docs/key-files.md](docs/key-files.md), not in this file** — `CLAUDE.md` is loaded into every session and has a 150,000-character budget, which its Key Files table had outgrown. Touch `CLAUDE.md` itself only for a new binding rule, a new command, or a one-line summary of a genuinely new file.
- **Before authoring any new homework (or editing an existing one's answers):** independently solve every problem and verify each baked-in answer is both **correct** and **complete** before committing it. Work each problem from scratch (compute numerics with a script, not by hand, to avoid arithmetic slips), inspect any referenced `figure` to confirm the values/angles you assume match what students see, and confirm `graph` key points lie on the curve the answer implies. Do not trust the source answer key — instructor keys are known to contain errors and omissions. Treat a `text`/`math` answer as incomplete unless it states the full reasoning a student is expected to give. Set an explicit `sigFigs` on **every** numeric answer/part (matching the precision of the problem's given data) so the revealed correct answer shows the right significant figures — a bare number literal silently drops trailing zeros (`9.00` → "9"). **Whenever your verified value differs from the instructor's source key — even within the ±2% tolerance — log it in [docs/answer-key-discrepancies.md](docs/answer-key-discrepancies.md)** — under that course's top-level section — (flagging whether the gap exceeds ±2%) so the instructor can fix the source key later. Record the per-assignment notes and the dated verification entry in the course doc ([docs/courses/phy115.md](docs/courses/phy115.md) or [docs/courses/phy215.md](docs/courses/phy215.md)). See [docs/homework-roadmap.md](docs/homework-roadmap.md) § Authoring.
- **Student-facing content is PLAIN TEXT plus `$…$` math — no markdown, no em-dashes.** `MathText` (the renderer for every prompt, guide label and revealed answer) only understands KaTeX delimiters; it has no markdown pass, so `**bold**` and `*italic*` reach students as **literal asterisks**. **`$…$` math is homework-only:** a **quiz** question's `text` is rendered raw, not through `MathText` (`ChatMessages.jsx`, the `msg.type === "question"` branch), so a `$q$` in quiz text reaches students as literal dollar signs — write quiz symbols as bare prose (`a point charge q`). Don't use them, and don't reach for `$\textit{…}$` either — the house style is no emphasis at all. Em-dashes (—) are also out: use a comma, colon, semicolon, or a new sentence. This covers homework prompts, `guide` labels/notes, quiz text/replies/`feedback` maps, the `answer` strings in `_answerKeys.js` (students see those on reveal), **and all app UI copy on both the student and instructor sides** — buttons, placeholders, modal text, tooltips/`title` attributes, feedback messages, course labels in `course-meta.js`, and seeded module item titles. En-dashes in compounds like `action–reaction` are fine. Three things are deliberately exempt: **code comments** (not user-visible), **Claude-facing prompt text** (`HINT_RULES`, the tutor/integrity system prompts, the syllabus-extraction schema — reworded copy there risks changing model behavior), The **no-data glyph** in tables and empty cells is a plain hyphen `"-"` (`{stu.email || "-"}`), not a dash character.
- **Set `figureWidth` on every problem that has a `figure`.** A figure with no `figureWidth` renders at its **natural pixel size** (capped only by the 960px column), and source images are textbook screenshots captured at arbitrary zoom — so natural size is almost never right for the page. Check the natural size (`sips -g pixelWidth -g pixelHeight <file>`), pick a width appropriate for the page (~360–440px for a typical square/landscape diagram, ~160–200px for a tall/narrow one, up to ~560px only when dense labels need it), and record the natural size in a trailing comment. See [docs/homework-roadmap.md](docs/homework-roadmap.md) § Authoring → Figures.
- **Homework prompts are the source problem's wording, VERBATIM.** Transcribe the textbook sentence; do not paraphrase it, add a formula or structural hint to the stem, append unit instructions (the `unit` field renders beside the input box already), or invent parts the source does not ask. Only four adaptations are allowed, all forced by the app: a figure reference becomes "the figure" (the served crops carry no caption), a part asking about "each capacitor" becomes one blank per capacitor named by the source's own label, units live in `unit`, and house style applies (plain text + `$…$`, no markdown/em-dashes). Near-identical quantities get **separate** blanks rather than one blank plus a note that they agree, whenever *that* they agree is what the problem tests. Anything you think should be added is a **recommendation in your summary and the course doc**, never an edit to the assignment. See [docs/homework-roadmap.md](docs/homework-roadmap.md) § Authoring → Wording; PHY 215 `hw4` is the reference set.
- **When authoring a homework set, explicitly analyze every problem/part for whether a standard deterministic numeric answer actually serves it well, and call out the exceptions** (in your summary to the user AND in the roadmap's per-assignment note). Watch for: (1) **ill-conditioned numerics** — answers hypersensitive to rounding or where the "correct" value is ≈0 (no meaningful ±2% band); grade these as a conceptual `text` part instead. (2) **questions whose real content is a diagram, sketch, or direction**, not a number — use `answerType: "graph"` (curves) or `answerType: "vector"` (arrows / free-body diagrams), or split a "magnitude and direction" question into a numeric magnitude + a `text`/numeric direction. (3) **answers that are expressions or multi-step reasoning** — use `math` (LaTeX) or `text`. Do not force a question into a numeric blank when one of these fits better; flag any part that no existing `answerType` serves so the user can decide. See [docs/homework-roadmap.md](docs/homework-roadmap.md) § Authoring.

## Quick Reference

- **Dev & deploy:** see [docs/environment.md](docs/environment.md)
- **System design, Firebase, auth, Claude API:** see [docs/architecture.md](docs/architecture.md)
- **Inline styles and theme constants:** see [docs/styling.md](docs/styling.md)
- **End-to-end testing, local-dev gotchas:** see [docs/testing.md](docs/testing.md)
- **LMS-style redesign (multi-session plan):** see [docs/lms-redesign.md](docs/lms-redesign.md)
- **Homework — process, engine & remaining buildout (course-agnostic):** see [docs/homework-roadmap.md](docs/homework-roadmap.md)
- **Per-course content notes (which problems, figures, why a part isn't numeric):** [docs/courses/phy115.md](docs/courses/phy115.md) (PHY 115 = `physics1`) · [docs/courses/phy215.md](docs/courses/phy215.md) (PHY 215 = `physics2`)
- **Score handling / instructor↔student override parity, and the Blackboard Grade Center export:** see [docs/grading-scores.md](docs/grading-scores.md)
- **Instructor analytics (correlation view, planned telemetry & item analysis):** see [docs/analytics.md](docs/analytics.md)
- **What every file is and why it is built that way:** see [docs/key-files.md](docs/key-files.md)

## Key Patterns

### Theme — `useTheme()` in every component
All components must call `useTheme()` — never import the static `s`, `BG`, `CARD`, etc. constants directly in components (those are App.jsx-only legacy usage):
```js
const { s, text, muted, border, teal, card, bg, isLight } = useTheme();
```
Every screen branch in `App.jsx` is wrapped in `<ThemeContext.Provider value={appTh}>` so all descendants pick up the correct light/dark state.

**Modal inner cards** must override `card`'s semi-transparent light-mode value with:
```js
const solidBg = isLight ? "#fff" : "#252627";
// <div style={{ ...s.card, background: solidBg, ... }}>
```

### Navigation — `screen` state, no router
The entire app is one component (`App.jsx`). Navigation is `setScreen("...")` state. There are no URL routes. Within portals, `studentSection` / `instructorSection` selects the active panel. Selectors in tests must use visible text, not URLs.

### Adding new Firebase per-class state
Every new Firebase node under `classes/{classId}/` requires changes in **three places** in `App.jsx`:
1. **Startup cache restore** (~line 300): `if (c.newThing && ...) setNewThing(c.newThing);`
2. **`loadClassData` Promise.all** (~line 344): add `fbGet(classPath(classId, 'newThing')).catch(() => null),` and destructure + normalize the result
3. **`setClasses` cache call** (~line 436): add `newThing: newThingObj` to keep the in-memory class cache in sync for fast class-switching

Also add the new node name to `database.rules.json` under `$classId`.

**Fourth place — if the node is instructor-authored and students consume it:** add it to `refreshClassContent` (App.jsx, right after `loadClassData`). There is no realtime listener (RTDB's REST stream is an `EventSource`, which can't send the App Check header), so a student's open portal only sees a node change if that node is re-polled. `refreshClassContent` re-pulls the instructor-owned nodes (`modules`, `moduleConfig`, `pages`, `uploads`, `customQuizzes`, `dueDates`, `homeworkSettings`, `announcements`, `gradeOverrides`, `syllabus`, `manualAssignments`) on entering the student portal, on tab focus, and every 60s while visible — this is what makes an instructor's module **reorder**/rename/add/hide/due-date edit show up for a student without a reload. It runs in the **student portal only** (instructor writes are optimistic, so polling there could momentarily revert an in-flight edit) and deliberately excludes every per-student node (`submissions`, `hwDrafts`, `hwAttempts`, `roster`, `studentPws`) so a poll can never clobber the student's own work. See [docs/architecture.md](docs/architecture.md) § Data freshness.

**Exception — `hwDrafts` / `hwAttempts` / `hwProgress` / `hwTelemetry` / `announcementReads` / `materialViews`:** student homework drafts (`classes/{classId}/hwDrafts/{studentId}/{hwId}`), authoritative per-item attempt counts (`classes/{classId}/hwAttempts/{studentId}/{hwId}`) and the instructor-facing completion summary (`classes/{classId}/hwProgress/{studentId}/{hwId}`) and the engagement telemetry (`classes/{classId}/hwTelemetry/{studentId}/{hwId}`) are written and read directly on demand (not loaded into the App.jsx class cache), since they are per-student — the first two by `HomeworkRunner.jsx`, `hwProgress` written by the runner and read by `Assignments.jsx` (the instructor's Progress column) and by `StudentGrades.jsx` (the signed-in student's own `hwProgress/{studentId}`, which is what tells them a past-due homework is still unfinished and still claimable — driven by their draft rather than by a deadline record, since the sweep that writes those is lazy and may never have run). `hwAttempts` is the anti-gaming source of truth: written on every submit **one item key at a time** (`hwAttempts/{studentId}/{hwId}/{itemId}`), seeded into local state unconditionally on mount, and cleared only on final submission (never by leaving/resuming). **`announcementReads`** (`classes/{classId}/announcementReads/{studentId}/{annId}`) is the same shape of exception: the signed-in student writes their own receipts one key at a time, and the instructor's Announcements tab reads the whole node lazily on entry (see `src/announcements.js`). **`materialViews`** (`classes/{classId}/materialViews/{studentId}/{itemId}` = `{ first, last, count }`) is the same shape again for course-material opens: the student's portal PATCHes ONE item's record per click, and the instructor's Analytics → Materials view reads the whole node lazily (see `src/material-views.js`). `recordMaterialView` also updates the local map **optimistically**, because the same record is what ticks the material off in the student's own module list and the tick has to land on the click, not on the next reload; it seeds the map even when the click beats the initial GET, and that GET merges its result *under* anything already there rather than over it, so the race cannot swallow a tick the student just earned. **`hwProgress`** is `{ done, total, pct, updatedAt }` and exists so the instructor can see how far the class has got **without** reading `hwDrafts`, which carries every typed answer, every feedback string and the whole Claude history per item; it is written by the same `persistDraft()` chokepoint as the draft (so the two cannot drift) and cleared alongside it on final submission, after which the submission is the record. The draft itself is PATCHed leaf by leaf rather than PUT whole, so two sittings merge instead of overwriting — see § *Write discipline* and `src/student-work.js`. See [docs/homework-roadmap.md](docs/homework-roadmap.md) § Instructor progress view.

### Write discipline — never PUT a composite you built from local state

There is **no realtime listener** (RTDB's REST stream can't carry the App Check header), so every session holds a snapshot from when it loaded, and student sessions stay open for hours. A `fbSet` of a whole object or array is therefore last-writer-wins across sessions: it does not merge, it **replaces**, and everything the other session added in between is gone with no error and no trace. On 2026-09-06 that destroyed a completed homework 95 minutes after it was submitted.

So, for any node more than one session can write:

- **Address the smallest thing you are changing.** One submission (`submissions/{studentId}/{submissionId}`), one attempt count (`hwAttempts/{sid}/{hwId}/{itemId}`), one password (`studentPws/{studentId}`), one roster field (`roster/{i}/email`). Not the collection that contains it.
- **When one write must span several keys, use `fbUpdate` / `fbSave(…, { merge: true })`** with deep-path keys, which PATCHes and leaves siblings alone. `buildDraftPatch` (`src/student-work.js`) is the worked example.
- **Never emit `null` in a merge patch to prune** something local state says is absent — that is the whole-object clobber wearing a different hat.
- **A positional path is only meaningful against the array you just read.** Re-read immediately before writing one, and prefer a leaf write, whose worst case is one wrong field rather than a dropped record. `keyForSubmission` exists because a submission's key is NOT always its id.
- **Adopt a write into local state only AFTER it lands** — for anything that gates access or records student work. `saveSettings` used to `setSettings` first and then await, so a failed write left that session logging in with an instructor password the database had never accepted (it worked until the tab was reloaded, while every other device still needed the old one). Report the failure in words and say what is still in effect.
- **Never write a credential that is printed in this repo.** The instructor password used to be seeded from a constant here on first run; the login now asks for one (`instLoginStep === "claim"`) and `LEGACY_SEEDED_PW` survives only to detect a database still answering to the old default and force its replacement.
- **Whole-node PUTs are for deliberate, password-gated destruction only** (the Danger Zone). If you are reaching for one to *save* something, the design is wrong.
- **Restores merge.** A backup is older than the live data, so importing one must add and update, never remove.

**Homework written-work integrity check:** before final submit, students must upload images/PDFs of their handwritten work (proof of original effort). These upload to Storage at `classes/{classId}/hwWork/{studentId}/{hwId}/{file}` and are recorded on the submission as `workFiles[]`; `checkWorkIntegrity` (homework.js) runs a lenient Claude sniff-check and writes `submission.integrity = { flagged, reason, checkedAt, model, error? }`. **A flag never withholds credit on its own:** a flagged submission counts at **full credit** (and the student sees their normal score) until the instructor opens it in the Gradebook and explicitly **upholds** the flag (50% penalty); the instructor may also **clear** it (an explicit full-credit record). The decision is stored at `gradeOverrides[studentId][hwId].integrityReview = "cleared"|"upheld"`. Penalty logic flows through the shared `integrityState` (returns `{ flagged, review, penalized }` — penalized only when upheld) / `integrityAdjustedScore` helpers (homework.js), used by both `Gradebook.jsx` and `StudentGrades.jsx`. The Gradebook still shows a red `*` marker on flagged cells so instructors know to review; students never see the AI verdict (the shared `SubViewModal` is passed `showIntegrity={false}` in the student view). No new RTDB node — it rides on `submissions` + `gradeOverrides`; only `storage.rules` gains the `hwWork` path.

## Key Files

Full detail for every row lives in [docs/key-files.md](docs/key-files.md) — read that row before changing a file, and record new rationale there rather than here.

| File | Purpose |
|------|---------|
| `src/App.jsx` | Top-level component — all state and screens. **`runnerFrom`** (`"student"` \| `"instructor"`) records who launched the quiz **or** homework runner: it picks the return screen and **forces practice when `from === "instructor"`**, since every per-student path reads `loggedInStudent`, which is null there. **Quiz question types are flags on the question object** (`requiresImage`, `yesNo`, `dragDrop`, `choices`, `survey`), each with its own submit handler and render branch. **Manual assignments** (`manualAssignments` — exams and labs, the work with no submission) are seeded per class and merged with `dueDates` into `manualAssignmentList`. **Every per-student write is per key, never per array** — `addSubmission`, `saveRosterFields`/`saveRosterField` (with its `guard`), `saveStudentNickname`, `saveStudentPw`; both delete paths archive to `submissionsArchive` first, and `saveRoster` collapses duplicate `studentId`s at the one chokepoint. **`classStudents`** (roster minus the `instructorAccount` entry) is what Gradebook / Analytics / Assignments / Attendance receive. Also here: `effectiveDue` application, `assignmentLocks`, `partitionModules`, `openMaterial`, `completedQuizIds` (filtered through `closesAssignment`), `refreshClassContent`, and the once-per-session deadline auto-submit sweep |
| `src/firebase.js` · `auth.js` · `theme.js` · `utils.js` | Extracted helpers (REST, hashing/TOTP, theme + `s` + `useTheme`/`ThemeContext`, dates/parsers/Claude API/`useIsMobile`). **`effectiveDue(due, override)`** resolves a per-student deadline extension over the assignment's date, applied in ONE place so it reaches every consumer. `dueToDate`/`fmtDueTime` accept both stored due-date shapes. **`fbUpdate(path, patch)`** is the REST PATCH counterpart to `fbSet` (deep-path keys, siblings untouched) — reach for it over `fbSet` for any node more than one session can write. Storage: `fbUpload`, `fbDeleteStorage`, `fbListStorage(prefix)` |
| `src/category-colors.js` | **Single source of truth for assignment-category colors** — `categoryColor(key, fallback)` and `categoryColorForName(name)`. Pure and env-agnostic. It exists because five colors were redeclared in four files and drifted. **Add a new category color here, never in a consumer** |
| `src/course-info.js` | **The non-coursework side of a class** — the reference SHELVES and the course GUIDE. A shelf is an ordinary module carrying `kind: "resources"`, not a separate node. `partitionModules` splits the merged list **at the render boundary only** — `mergedModules` stays COMPLETE, since analytics and locks read every module. `visibleShelves` decides what a shelf lists and whether the sidebar offers the page; `normalizeGuide`/`guideHasContent` do the same for the guide. Tested |
| `src/screens/student/StudentResources.jsx` · `CourseGuide.jsx` | The two student-facing halves of `course-info.js`. **Resources** reuses Home's `ModuleItem` rows with `tracked: false` — same look, no completion circles, no counter, no release lock; opens are still recorded. **CourseGuide** renders the weekly rhythm as a numbered timeline, and **a step is colored only when it names something the app actually tracks** (via `categoryColor`) — never give an off-app step a hue |
| `src/screens/instructor/InstructorGuide.jsx` | Authoring for the course guide (`instructorSection === "guide"`) — the instructor pastes prose, Claude structures it, everything stays hand-editable. Structuring **merges into** the current draft rather than replacing it. The preview is the real `CourseGuide` component |
| `src/components/AutoLink.jsx` | `renderWithLinks(text, color)` — turns bare URLs in plain instructor prose into real links (house style has no markdown, so there is no `[…](…)` to parse). Shared by `StudentSyllabus` and `CourseGuide`. In a `.jsx` file because it returns JSX |
| `src/analytics.js` | **Instructor analytics derivations** — no React/DOM. Three groups: **`buildScoreMatrix`** (the student × assignment grid of effective scores, every cell through `resolveScore`; extracted FROM `Gradebook.jsx`, which now imports it) plus `countsTowardGrade` ("no score ≠ zero") and **`isRecordedZero`**, which decides when a blank cell is really a zero — past due, nothing handed in — so the gradebook and the student's row print the 0 the arithmetic already used, derived and never stored; **the statistics** (`pearson` — returns `null`, never `0`, for <3 pairs or zero variance — `linearFit`, `correlationCI`, `buildCorrelations`, the `PREDICTORS` incl. `materials`, and deliberately no p-value); and **the phase-3 derivations** (`buildItemAnalysis`, `buildActivityByDay`/`buildActivityByHour`, `activeNow`, `buildFunnel`, `lastActiveMap`, `timeOnTaskMap`, `mergeTelemetry`). Tested |
| `src/hw-telemetry.js` | **Homework engagement accumulator** behind `hwTelemetry/{studentId}/{hwId}` — per-item active/hidden/unfocused time, paste count, and a bounded `attemptLog`. Three rules make the numbers mean anything: **active time excludes time away**; **`hiddenMs` and `unfocusedMs` are disjoint and NEVER summed**; **excursion shape beats excursion total**. It cannot see a phone, so away time must never be a standalone ranked column. Tested |
| `src/screens/instructor/Analytics.jsx` | Analytics tab **shell** plus the **Correlation** view. It **scopes every per-student input to the roster it is handed** (`src/roster-scope.js`) — the whole-node reads never iterate the roster themselves — merges telemetry via `mergeTelemetry` (it lives both in the live node and on submissions), and lazily loads only the nodes a chosen view needs. Correlation: pick an outcome, pick a predictor (score / attempts / time / materials opened), diverging bars + a square scatter with coincident points sized by count |
| `src/screens/instructor/analytics-ui.jsx` | **Shared chart palette and marks for all four Analytics views** — the diverging pair, the four-slot `series(isLight)` (its ORDER is load-bearing for adjacent stacked segments), the sequential `ramp`, and `Stat`/`Meter`/`StackedBar`/`Legend`/`ViewTabs`/`Panel`/`EmptyCard`. **Every color was run through the data-viz validator in light AND dark — re-run it (command at the top of the file) before changing any.** `Panel` deliberately has no `subtitle` slot |
| `src/screens/instructor/AnalyticsItems.jsx` | Analytics → **Items**: per-problem statistics for one homework. The argument is **mean × discrimination together**. Common wrong answers is the most actionable column, guarded so a value is listed only if two or more students gave it and each student contributes once |
| `src/screens/instructor/AnalyticsStudents.jsx` | Analytics → **Students**: the class across the term plus a per-student drill-down. **Deliberately no risk score** — it shows the components, sorts by overall grade, and badges only plain facts. A homework row opens `StudentWorkDetail` |
| `src/screens/instructor/StudentWorkDetail.jsx` | **One student's engagement on one homework** — time, sittings, per-problem table, and behind a row click the **attempt trail** (what they actually typed), which is the teaching payoff of the telemetry node. Takes telemetry as a **prop**; reads nothing itself |
| `src/screens/instructor/AnalyticsPulse.jsx` | Analytics → **Pulse**: a "Working now" tile (recorded active, never online; submitters excluded), students active per day, a day × hour heatmap, a completion funnel over assignments students can actually open, and a quiet-students list. **The funnel's "Finished, not handed in" bucket is why this view exists.** No explainer chrome — the panels' own furniture carries it. Re-reads every 60s while visible |
| `src/screens/instructor/AnalyticsMaterials.jsx` | Analytics → **Materials**: who has clicked to open the posted readings, notes, links and pages. **The view's job is to stop anyone reading an open as a read** — every figure is worded as an open, the caveat is on the panel, and the tracking start date is printed. No composite engagement score |
| `src/attendance.js` | **Lecture attendance + the lab-credit policy** — pure, storage at `attendance/{date}`. `buildAbsenceMap` is the chokepoint for the three rules that keep the policy from firing wrongly: a session counts only once `takenAt` is stamped, only a `labId` link gates anything, and only an affirmative `"absent"` mark counts. Also `attendanceFor`, the status constants, and hand-parsed dates |
| `src/lab-schedule.js` | **The term's lab sessions** — pure, tested. Two a week for every LECTURE week, since **midterm week holds no lab**: `labWeeksFor` is **per course** (PHY 215 teaches 13, so 26 sessions; PHY 115 teaches 14 and is deliberately NOT shortened with it, and an unlisted course keeps 14 — too few labs cannot be added back, there being no add control). Numbered CONSECUTIVELY (`Lab 1a`..`Lab 13b`) because midterm week falls in a different place in each course, so week-aligned numbers would make `Lab 8a` mean two different sessions. Labs are the one graded category seeded WHOLE up front (a lab is not authored in the app), so the seeded count IS the schedule and a wrong one is wrong in the gradebook's possible points, the absence policy and the student's grade projection at once. **`trimmableLabs` is the only thing in the app that deletes an assignment from a live gradebook** (a ONE-TIME migration for classes seeded with a 14th pair, carrying its own removal checklist and meant to be deleted once every class has loaded once) — it refuses unless the stored labs are exactly the seeded shape and the extra pair carries no grade record and no roll call, and it cancels the whole trim rather than deleting the half it is sure about |
| `src/screens/instructor/Attendance.jsx` | Attendance tab — **Take roll** (today's session only; a past day is reached by clicking its History column) and **History** (an editable grid whose cell menu writes immediately). Deliberately **no "mark all present"**, and Save is disabled until every student is marked; marks autosave as tapped but the session zeroes nobody until "Save roll call" stamps `takenAt`. Responsive — roll gets taken on a phone |
| `src/blackboard.js` | **Blackboard Grade Center interchange** — pure, tested. Import-once, export-many: a download carries the two facts that cannot be derived (the **Username** Blackboard matches on, and the **column id** that routes values into an existing column). `createMissing` (on) exports unlinked assignments under their bare title so Blackboard creates them; `scaleToColumn` (off) is the alternative to fixing points by hand. Never exports calculated columns, read-only columns, or excused cells |
| `src/course-meta.js` | **Course identity, content-free** — `COURSE_META[courseType] = { label, code }` + `courseLabelFor`. Pure, so both the client and the Netlify grading function import it without bundling course content |
| `src/courses/` | Per-course content (`physics1.js` = PHY 115, `physics2.js` = PHY 215). Quizzes + modules + homeworks; a problem is `{ id, prompt, figure?, figureWidth?, freeHint?, answerType, answer, graph?/vector?/fbd?, parts? }`. **Both courses use $g = 9.81$ m/s².** `MODULES_PHYSICS2` is deliberately empty. Served figures go in `public/homeworkFigures/<courseType>/HWn/`; instructor source material stays out of the repo |
| `src/components/lms/DueDateField.jsx` | **The one due-date editor**, shared by the Modules editor and the Assignments hub. A date picker + a separate time control + a badge, deliberately **not** `<input type="datetime-local">`, which renders blank for a stored date-only value and cannot express the default 11:59 PM. **`colorScheme` on native date/time inputs is required, not cosmetic** — the browser's calendar panel is not our DOM and no CSS of ours reaches inside it |
| `src/components/lms/itemIcons.jsx` | `<ItemIcon type size>` — the **single source of truth for module-item type icons**, shared by student and instructor rows. Also `<LockIcon>`, used wherever a timed release gates something |
| `src/components/lms/` | LMS layout blocks (Shell, Sidebar, TodoRail, ModuleList). **Module completion covers everything in a module**, not just graded work: `ModuleItem` draws its circle for any item whose `meta.tracked` is true and the header counter counts exactly those (`tracked` is `resolveItem`'s judgment, never a type test here); a material is completed by opening it. `TodoRail` is in both portals: the student rail keeps past-due work in a red section, keeps release-locked work but not clickable, and **drops labs entirely** (no makeups, nothing to act on); the instructor rail is upcoming-only and Home-only. Shell is a `100vh` flex column with a sticky in-scroll `footer` slot; fully responsive |
| `src/components/ChatMessages.jsx` | Quiz chat message renderer — also used by the shared `SubViewModal` |
| `src/components/SubmissionView.jsx` | Shared full-screen submission viewer (`SubViewModal`), used by both the Gradebook and `StudentGrades`. Renders the quiz dialogue OR a homework per-problem/per-part breakdown plus the uploaded written work; the header score comes from the shared `resolveScore`. **Instructor-only controls are gated by callback props** (part scores, integrity Clear/Uphold); banners for a deadline auto-submission (worded via `audience`) and a reconstructed record; `showIntegrity={false}` in the student view |
| `src/components/DragDropQuestion.jsx` | Drag-and-drop fill-in-the-blank quiz widget |
| `src/components/ChoiceQuestion.jsx` | Multiple-choice quiz widget (`choices: true`) — graded deterministically in `submitChoice`, with per-wrong-option `feedback` and the same 5-attempt/half-credit rule as free response |
| `src/screens/student/` | Student pages (Home, StudentCalendar, StudentGrades, CourseEvals, StudentSettings, Stub). **`Home`'s `resolveItem` is the one place that decides what a module contains and what finishing it means** (`tracked` + `completed`); every material opens through App.jsx's single `openMaterial`; its `modules` prop is `courseModules`, and there is deliberately nothing above the list. `StudentCalendar` events are clickable only when the assignment is open to that student (`locks`); the instructor's calendar passes `previewAssignment` instead. `StudentGrades` shows manual rows only once scored, applies the absence policy through `resolveScore`, badges the reasons a student would otherwise email about, opens submissions read-only, and shows the what-if panel under the overall banner. `StudentSettings` is a **page inside `Shell`**, one card per independent write |
| `src/grade-scenarios.js` · `src/screens/student/GradeScenario.jsx` | **The student's what-if panel**, live to students under the overall-grade banner (the `SHOW_GRADE_SCENARIOS` flag it waited behind is gone; it self-hides when no category has work left). One slider per category with work left, read against the real grade. **The projection routes through the SAME `calcGrades` as the banner above it** (drop-lowest re-runs, weights renormalize). **`plannedRemaining` makes a slider cover the rest of the TERM, not the rest of what has been built** — a category's `plannedCount` (13 homeworks, 13 quizzes) is a **floor that real assignments consume**, filled with placeholders that never leave the projection, so the total holds steady as homework 7 is written and then graded. A category absent from the sliders contributes its graded work, never a zero; sliders start at the student's current pace. Tested |
| `src/screens/student/StudentSyllabus.jsx` | Student syllabus display — renders `syllabus.fields` as cards; `showHeader` for the instructor view |
| `src/screens/instructor/Modules.jsx` | Instructor module editor — add/reorder/hide/rename items, custom quizzes, due dates via the shared `DueDateField`, **⋮ → + Homework** (the only way a hand-built class gets homework students can see). Rows carry the same meta line students see. **Clicking an item's title opens it** for every openable type — quiz/homework run the REAL student runners in preview; an item with nothing behind it gets no click. Only the title is a target (the row is the drag handle). **⋮ → "Show on Resources page"** sets `kind: "resources"`. Hidden file inputs are mounted at the root so a menu click opens the OS dialog synchronously; `file` rows offer Replace-in-place |
| `src/screens/instructor/Assignments.jsx` | Instructor assignments hub — view/filter/sort quizzes, homework and manual assignments; due dates through the shared `DueDateField`; manual rows get their date and editable **Points** here; homework rows get `HwGradingModal`. **Clicking a quiz/homework title opens the real student runner in instructor preview**, as in the Modules editor; a manual row has nothing to open and stays plain text. The **Progress** column covers quizzes too, where the bar is the share of the class that HANDED IN rather than average completion (all-or-nothing work has no partial state, so the wording and the modal's stats differ by kind). It reads `hwProgress` **and `submissions` together** on a 60s visible-only refresh (the join is wrong the moment one side is stale — a submitted student would read as 0%) and never reads `hwDrafts`; percent is weighted by problem. Below 768px the table becomes a card list built from the same cells |
| `src/screens/instructor/Gradebook.jsx` | Instructor gradebook — builds its maps via the shared `buildScoreMatrix`/`countsTowardGrade`, so the grid, the student's grades list and Analytics cannot disagree. Weighted categories, CSV + Blackboard exports, excuse/unexcuse, per-student deadline extensions, Clear Submission, per-part regrade, integrity Clear/Uphold, attendance waiver, "Accept without written work". **An untouched cell commits nothing** and a score that changes nothing is never stored — an override should exist only where it overrules something. Points are per-assignment (exams /100), so cells shade by percentage |
| `src/homework.js` | **Homework grading engine** — `itemsOf`, `HW_GRADING_DEFAULTS` (3 free attempts, hint at 80%, reveal at 0%, ±2% numeric), `creditForAttempt`, the sig-fig-agnostic `numericMatch` and `formatNumericAnswer`, and `evaluateHomeworkAnswer`. **Numeric/text/math grade SERVER-SIDE** (`netlify/functions/grade.js`) so answers never reach the client; **graph/vector/fbd grade client-side and deterministically** (`gradeGraph`, `gradeVectors`, `gradeFBD` + their hint/reveal/key helpers). Angular tolerance is **±5° everywhere** — check a field's snap grid before tightening one. `snapVectorMagnitudes`/`snapFBDDirections` fix presentation only, after a verdict. **`resolveScore(submission, override, attendance, due)` is the single source of truth** for the score a cell shows: `excused` > lecture absence > whole override > part overrides (on-time subtotal unhalved) > submission score, then the upheld-integrity penalty, then the deadline auto-submission gate. **The late penalty is applied only to the parts finished after the deadline** — `onTimeCreditIds` unions the `onTime` stamps on the record with the parts the submission's own telemetry says were resolved before `due` (pass `effectiveDue(assignment.dueDate, ov.dueDate)`), derived on every read so it needs no migration and reaches homework already submitted. Also the written-work integrity check (`checkWorkIntegrity`, `integrityState`, `integrityAdjustedScore`) |
| `src/screens/student/HomeworkRunner.jsx` | MasteringPhysics-style homework runner (`screen === "homework"`). One `persistDraft()` chokepoint writes the draft, `hwProgress` and telemetry so the three cannot drift; **the draft is a deep-path PATCH** and **attempt counts are written one leaf at a time**, because a second sitting must merge, not overwrite. Typed-but-unsubmitted answers autosave (debounced, flushed on hide/unmount); `submittedRef` blocks every save path after submit. Prompts render through the non-copyable `Prompt` component; `freeHint` is a free amber callout that never touches credit; numeric input accepts scientific notation with no instructions. Multipart problems reveal parts in place, with auto-scroll, auto-focus and a delayed advance-button glow; an instructor `preview` shows every part at once (`lastVisibleIndex`), while `workingIndex` keeps the focus/scroll/telemetry target on the first unresolved part. **Graph/vector/FBD items have no Submit** — they grade on every placement, freeze what passes, and offer a free Hint (caps credit) or Show answer. Finish opens the mandatory work upload (files normalized at attach time) then the integrity check. **No-lost-work guarantee:** every exit preserves the draft, and it clears only on submit success. `practice` (not `preview`) is what keeps a run off every per-student path |
| `src/components/MathField.jsx` · `MathText.jsx` | `MathField` = controlled MathLive LaTeX editor (lazy-loaded); `MathText` = KaTeX renderer for `$…$` in prompts and reveals. **The virtual keyboard is dismissed by us** (`hideMathKeyboard()`) on focusout, disable, unmount and explicitly after a math submit — it is a window-level singleton nothing else hides |
| `src/components/InfoDot.jsx` | Small circled-"i" click-away popover for verbose tool mechanics in the graphical fields |
| `src/components/GraphField.jsx` | Controlled SVG plotter for `answerType: "graph"` — click to add a point, drag to move, click to remove, plus a per-curve shape picker; `curveUp`/`curveDown` render as a **true parabola** via least-squares `quadFit`. **Rendering never affects grading** (`gradeGraph` interpolates the anchors piecewise-linearly). `readOnly` renders the reveal and the gradebook. The `grade.pass` map both greens and **freezes** each solved curve; optional `guide` checklist with three states |
| `src/components/VectorField.jsx` | Controlled SVG arrow plotter for `answerType: "vector"` — arrows from a common origin, `freeTail` two-click vectors (so a graphical subtraction grades the same anywhere), `square`/`hideGrid`/`hideTicks`, a `dial` clockface (geometry from `src/clock-geometry.js`), and `render: "axis"` for an undirected line placed by orientation. Same freeze/guide/readOnly conventions as `GraphField`; graded by `gradeVectors` |
| `src/components/FBDField.jsx` | Controlled **free-body-diagram builder** for `answerType: "fbd"` — a force BANK rather than a fixed list (any number of each type, auto-subscripted, one color), an off-to-the-side acceleration arrow or equilibrium toggle, and a rotatable positive-axes gizmo that is a required but ungraded step. **The plot is square on purpose** — unequal px-per-unit grades an off-axis force at a different angle than the student sees. Off-axis forces are annotated automatically; `angleSymbol` names the angle instead of measuring it when it is the problem's unknown. Graded live by `gradeFBD` |
| `src/components/VectorBuildup.jsx` | Animated read-only illustration decomposing a keyed vector into `count` equal tip-to-tail steps; shown after a vector part resolves |
| `src/screens/instructor/InstructorSyllabus.jsx` | Instructor syllabus — upload/replace/remove PDF, mismatch warning, full content via `StudentSyllabus` |
| `netlify/functions/claude.js` | Claude API proxy — reads `CLAUDE_API_KEY` (NOT `ANTHROPIC_API_KEY`, which the Netlify Anthropic extension overrides) |
| `netlify/functions/grade.js` | **Server-side homework grading endpoint** for numeric/text/math, so answers never reach the client. `action:"grade"` → `{ correct, message, revealedAnswer?, retry? }` (revealed only when resolved; tolerance is server-owned); `action:"reveal"` fills the gradebook key at final submit |
| `netlify/functions/_answerKeys.js` | **Server-only answer key** (leading `_` ⇒ helper, not an endpoint), imported only by `grade.js`. Per-item `{ answerType, answer, sigFigs?, unit?, tolerance?, sci?, nonNegative?, angle? }`. `nonNegative` turns a negative entry into a **no-cost retry nudge** — never set it where the sign IS the physics; `angle` folds coterminal spellings; the two are mutually exclusive. **When authoring or editing a problem, the answer goes HERE** (prompt/figure/configs stay in `src/courses/`) |
| `src/grading-core.js` | Pure grading primitives shared by client and server: `normalizeSciNotation` (every spelling students type), `parseNumber`, `sigFigsOf`, `numericMatch`, `angleMatch`, `toSigFigString`, `toSciString` (Unicode superscripts, opt-in per answer via `sci`), `normalizeLatexForGrading` (re-braces MathLive's compact `\frac35`), `parseJsonReply`/`extractJsonObject` (hardened against LaTeX-broken JSON), `HINT_RULES` |
| `src/work-files.js` | **Work-file normalization for the written-work upload** — rasterizes an oversized photo-PDF at ATTACH time (a phone's "Create PDF" is ~145 MB; verified 134× smaller, handwriting legible). Two ceilings: `WORK_HARD_LIMIT` (25 MB, mirrors `storage.rules`) and `WORK_TARGET_BYTES` (2.5 MB, so the integrity check's POST still fits). A file already under target passes through untouched; `normalizeWorkFile` never throws. Its hand-written PDF assembler's xref offsets are what the tests really guard. Tested |
| `src/clock-geometry.js` | Pure clockface geometry (`clockAngleDeg`, `clockPoint`), imported by BOTH `VectorField` and `courses/physics2.js` so the dial and the key cannot drift |
| `netlify/functions/instructor-reset.js` | **Emailed instructor password reset** — stateless: `request` mints a code, mails it to the server-side instructor address and returns a signed expiry (never the code); `verify` recomputes the HMAC. **FLAGGED: `verify` is advisory** (the browser does the write, since the rules let any client write `settings`) and the address is ONE constant. Both must change before a second instructor exists — see [docs/architecture.md](docs/architecture.md) § Instructor password recovery. Tested |
| `netlify/functions/send-email.js` | Email broadcast proxy (Resend) — also owns the announcement email template, mirroring the in-app card, with real `<p>` blocks (Outlook ignores `white-space`) and a plain-text alternative. **Each student is the sole recipient of their own copy**: the roster is fanned out into one message per address via the batch endpoint, deduped case-insensitively |
| `src/components/lms/PageEditor.jsx` | Modal for creating/editing pages and custom quizzes — shared via `editorLabel`/`contentLabel` |
| `src/components/ManualAddStudent.jsx` | Instructor roster manual-add form (name, ID, email) |
| `src/components/lms/AnnouncementEditor.jsx` | Announcement compose modal — title, body, email checkbox |
| `src/auto-submit.js` | **Deadline auto-submission of homework** — pure, tested. When a deadline passes over a draft holding finished work, that work is written down. **The record is a RECEIPT, NOT A GRADE:** it is worth 0 until the written work arrives (`workPendingState`), the draft is NOT cleared and the assignment stays open (`closesAssignment` is what makes that real), every part is stamped `onTime` so those parts keep full credit when the student does hand in, and `AUTO_SUBMIT_SINCE` stops the first sweep manufacturing records for a term of past drafts |
| `src/auto-submit-sweep.js` | The impure half of `auto-submit.js`. **A LAZY sweep, not a cron** — every RTDB write comes from a signed-in browser, so a scheduled function has no credential path. It runs when the student's portal or the instructor's opens the class; both write the same deterministic key. **The instructor's pass never reads `hwDrafts` wholesale** — candidates come from `hwProgress`, and `draftHasCompletedWork` is the authoritative re-check and the race guard |
| `src/student-work.js` | **The storage shapes of student work, and the merge-safe patches that write them** — pure, tested. One rule: **a write that saves student work must never be a whole-object PUT built from local state** (on 2026-09-06 that destroyed a completed homework 95 minutes after submission). Submissions are one key each; `flattenSubs` reads both the per-key and legacy array shapes; **`keyForSubmission` is the one that is easy to get wrong** (under the legacy shape the key is a position, not the id). `buildDraftPatch` is the additive deep-path draft patch, emitting no nulls |
| `src/announcements.js` | **Announcement read receipts** — pure, tested. `readAtOf`, `unseenAnnouncements`, `readReceiptsFor`, and `READ_TRACKING_SINCE`, which keeps the feature honest at both ends. A receipt is deliberately not cleared when an announcement is edited |
| `src/material-views.js` | **Course-material open tracking** — pure, tested. `materialsOf`, `openersOf`/`materialStats`, `materialOpensByStudent`, `materialModules`. **A click is an OPEN, never a read.** Three rules keep counts honest: a placeholder is not material, a hidden item is not material, and `MATERIAL_TRACKING_SINCE` is stated in the UI |
| `src/roster-scope.js` | **Which roster entries are actually students** — pure, tested. Two reasons an entry is not one: **`instructorAccount: true`** (exactly one per class, set by the "My own account" picker) and **`auditing: true`** (any number, toggled on the student's own row — someone following the course unofficially, with no grade to earn and no attendance to keep). **`isCountedStudent` is the single predicate** `studentRoster` filters by, so a third reason is added there and nowhere else. Both still write submissions and telemetry like a student and see their own grades, but are scoped out of the four class views via `studentRoster`/`scopeSubmissions`/`scopeByStudent`. Neither is a "hide this student" flag, and neither applies where the roster means who can log in or who to email |
| `src/nickname.js` | **Student-chosen preferred first names** — pure (both the browser and `screen-name.js` need the rules), tested. Writes the **same `altName`** the instructor edits, so the app has one display name; `altNameFor`/`nicknameFromAltName` pair it with the real surname. Only the structure lives here — the wordlist and the judgment are server-side. Deliberately loose about which letters |
| `netlify/functions/screen-name.js` | **Server-side approval for a student-chosen display name**, which is not private (it shows on the pre-login picker). A wordlist floor, then **Claude** as the real filter, and it **FAILS CLOSED**. Two rules stop the wordlist becoming the bigger problem: repeat-collapsing is substring-list only, and short collapses are skipped; genuinely ambiguous words are left to Claude |
| `src/components/lms/NewAnnouncementsModal.jsx` | The student's unread-announcement popup. **Every way of closing it marks the shown announcements read**, or it would re-pop and leave the instructor's receipts empty. Suppressed on the Announcements section and on Settings; App.jsx keys the receipts by `{ classId, studentId }` |
| `src/components/BugReportModal.jsx` | Bug report submit modal — rendered on login screens, student portal and settings |
| `src/components/Wordmark.jsx` | The **"Newton" wordmark**, and the app's home button — one component so the login heading and the portal mark cannot drift. Passing `onClick` makes it a button; the centered login wordmarks take none |
| `src/components/Footer.jsx` | Bottom footer (copyright, GitHub link, bug report). Default is `position: fixed` for the pre-portal screens; **`inline`** is the in-flow row for `Shell`'s sticky footer slot |
| `database.rules.json` | Firebase RTDB security rules |
| `storage.rules` · `firebase.json` | Firebase Storage rules and CLI config (deploy with `firebase deploy --only storage` / `--only database`) |
| `netlify.toml` | Netlify build config |
| `public/favicon.svg` | Browser tab icon — hand-crafted SVG apple (red body, green leaf, brown stem) |

## Commands

```bash
node src/hw-telemetry.test.mjs   # telemetry accounting (plain node, no framework)
node src/analytics.test.mjs      # item statistics, discrimination, funnel, activity window
node src/due-dates.test.mjs      # due-date parsing + per-student deadline extensions
node src/blackboard.test.mjs     # Blackboard CSV round trip, column-id + username matching
node src/work-files.test.mjs     # written-work PDF assembler: xref offsets, JPEG passthrough, page fitting
node src/announcements.test.mjs  # announcement read receipts: legacy flag, tracking baseline, viewed/not-viewed split
node src/student-work.test.mjs   # submission/draft storage shapes: both node shapes, key resolution, merge-safe patches
node src/material-views.test.mjs # course-material opens: what counts as material, stored-shape normalization, per-student totals
node src/nickname.test.mjs       # student-chosen preferred names: format rules, altName round trip, wordlist false positives
node src/roster-scope.test.mjs   # which entries are students: instructorAccount defaults, roster + per-student node scoping
node src/auto-submit.test.mjs    # deadline auto-submission: baseline, per-student extensions, the never-late stamp, per-part on-time credit
node src/course-info.test.mjs    # resource shelves + course guide: the module partition, what a shelf lists, guide normalization
node src/grade-scenarios.test.mjs # student what-if projections: drop-lowest re-run, weight renormalization, a category left out is not a zero
node src/lab-schedule.test.mjs  # the term's lab count, and every guard that stops the stale-lab trim deleting a graded lab
node src/instructor-reset.test.mjs # emailed instructor password reset: expiry, forged/stretched challenges, fail-closed signing key (sends no email)
npm run dev      # Vite dev server (port 5173) — NOTE: homework grading needs the functions, use `netlify dev`
netlify dev      # Vite + Netlify Functions (REQUIRED to test homework grading + Claude proxy: numeric/text/math now grade via netlify/functions/grade.js)
npm run build    # production build → dist/
```

No test runner or linters. The fifteen exceptions are `src/hw-telemetry.test.mjs`, `src/analytics.test.mjs`, `src/due-dates.test.mjs`, `src/blackboard.test.mjs`, `src/work-files.test.mjs`, `src/announcements.test.mjs`, `src/student-work.test.mjs`, `src/material-views.test.mjs`, `src/nickname.test.mjs`, `src/roster-scope.test.mjs`, `src/auto-submit.test.mjs`, `src/course-info.test.mjs`, `src/grade-scenarios.test.mjs`, `src/instructor-reset.test.mjs` and `src/lab-schedule.test.mjs` (run each with plain `node`), which exist because their output is silently plausible when wrong and an instructor may judge a student, drop a problem from the course, halve a mark, upload a corrupt file to the official gradebook of record, open a student's only copy of their written work and find an unreadable file, tell a student they never read an announcement they did read, believe a student never opened a reading they did open, find that a submission they graded yesterday no longer exists, tell a student their own name is inappropriate, read a class statistic computed over a test account that is not a student, find that a homework a student worked before the deadline was recorded late, locked out of being finished, or halved on parts they finished on time, post a module that disappears from both the student's Home page and their Resources page at once, let a student plan the rest of their term around a projected grade the gradebook will never produce, delete a lab a student was already graded on, or hand the instructor password to whoever asked for a reset code, on the strength of it. Deploy on `git push` via Netlify CI. For manual verification of changes, see [docs/testing.md](docs/testing.md).

Runtime deps beyond React: `qrcode`, `mathlive` (homework math/LaTeX input, lazy-loaded), `katex` (math rendering — its CSS is imported in `src/main.jsx`), `pdfjs-dist` (rasterizing oversized written-work PDFs in `src/work-files.js`, lazy-loaded — it is code-split into its own chunk plus a worker asset, so the main bundle is unchanged).
